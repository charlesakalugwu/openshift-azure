apiVersion: v1
kind: Service
metadata:
  labels:
    k8s-app: azure-controllers
  name: azure-controllers
  namespace: openshift-infra
spec:
  ports:
  - name: http
    port: 8080
  selector:
    k8s-app: azure-controllers
